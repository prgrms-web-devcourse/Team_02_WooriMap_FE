import styled from '@emotion/styled';

export const FormLabel = styled.label`
  width: 4.5rem;
  height: 1.25rem;

  margin-top: auto;
  margin-bottom: auto;

  font-size: 0.9rem;
  font-weight: bold;
  word-break: break-all;
  line-height: 1.5rem;
  text-align: left;
`;
