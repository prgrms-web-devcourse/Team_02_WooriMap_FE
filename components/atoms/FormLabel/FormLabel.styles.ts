import styled from '@emotion/styled';

export const FormLabel = styled.label`
  width: 52px;
  height: 20px;

  margin-right: 10px;
  margin-top: 14px;

  font-size: 14px;
  font-weight: bold;
  word-break: break-all;
  text-align: center;
`;
