import styled from '@emotion/styled';

export const FormLabel = styled.label`
  width: 52px;
  height: 20px;

  margin-top: auto;
  margin-bottom: auto;

  font-size: 14px;
  font-weight: bold;
  word-break: break-all;
  text-align: center;
`;
